# Test environment variables for TL Showroom Backend POC
# These are used for running tests locally

# Database configuration for testing
# Using SQLite for tests to avoid needing a PostgreSQL server
TEST_DATABASE_URL=sqlite:///./test.db

# If you want to use PostgreSQL for testing, uncomment and configure these:
# TEST_DB_USER=test_user
# TEST_DB_PASS=test_password
# TEST_DB_NAME=test_db
# TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db